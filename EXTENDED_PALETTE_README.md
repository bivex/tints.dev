# Extended Palette Generator

Генератор расширенных цветовых палитр для комплексных UI дизайн-систем.

## Что генерируется

Для каждой палитры создается полный набор настроек:

### 🎨 Основная шкала цветов
- Стандартные stops: 50, 100, 200, 300, 400, 500, 600, 700, 800, 900, 950
- Использует OKLCH цветовое пространство для лучшей точности

### 🎯 UI состояния
- **Hover**: Немного темнее для hover эффектов
- **Active**: Еще темнее для активных состояний
- **Focus**: Повышенная насыщенность для фокуса
- **Disabled**: Очень светлые и бледные для отключенных элементов

### 🌈 Варианты
- **Light**: Светлая версия палитры
- **Dark**: Темная версия палитры
- **Accent**: Акцентная версия с смещенным оттенком

### ✨ Градиенты
- **Primary**: Основной градиент (400 → 600)
- **Secondary**: Вторичный градиент (300 → 500)
- **Accent**: Акцентный градиент (200 → 700)
- **Subtle**: Нежный градиент (100 → 300)

### 🌑 Тени
- Полный набор теней: sm, base, md, lg, xl
- Используют базовый цвет палитры с прозрачностью

### 🔍 Прозрачности
- Alpha каналы от 10% до 90%
- Для создания полупрозрачных фонов

## Как использовать

### 1. Через веб-интерфейс
```
http://localhost/extended-palette/pastel-analogous-2
```

### 2. Через API
```bash
curl "http://localhost/api/extended-palette/pastel-analogous-2"
```

### 3. Через CLI (после сборки)
```bash
node scripts/generate-extended-palette.js pastel-analogous-2 #8B8B8B
```

## Примеры использования в Tailwind

### Основные цвета
```html
<button class="bg-pastel-analogous-2-500 text-white hover:bg-pastel-analogous-2-ui-hover-500">
  Button
</button>
```

### UI состояния
```html
<input class="
  bg-pastel-analogous-2-100
  border-pastel-analogous-2-300
  focus:bg-pastel-analogous-2-ui-focus-100
  focus:border-pastel-analogous-2-ui-focus-500
  disabled:bg-pastel-analogous-2-ui-disabled-100
  disabled:border-pastel-analogous-2-ui-disabled-300
" />
```

### Градиенты
```html
<div class="bg-pastel-analogous-2-gradient-primary p-6 rounded-lg">
  <h2 class="text-white">Gradient Background</h2>
</div>
```

### Тени
```html
<div class="bg-white shadow-pastel-analogous-2-lg p-4 rounded">
  Card with custom shadow
</div>
```

### Прозрачности
```html
<div class="bg-pastel-analogous-2-alpha-20 backdrop-blur-sm">
  Semi-transparent overlay
</div>
```

## Структура конфигурации

```javascript
module.exports = {
  theme: {
    extend: {
      colors: {
        "pastel-analogous-2": { /* Основная шкала */ },
        "pastel-analogous-2-ui": {
          hover: { /* Hover состояния */ },
          active: { /* Active состояния */ },
          focus: { /* Focus состояния */ },
          disabled: { /* Disabled состояния */ }
        },
        "pastel-analogous-2-light": { /* Светлый вариант */ },
        "pastel-analogous-2-dark": { /* Темный вариант */ },
        "pastel-analogous-2-accent": { /* Акцентный вариант */ }
      },
      backgroundImage: {
        "pastel-analogous-2-gradient-primary": "...",
        // ... другие градиенты
      },
      boxShadow: {
        "pastel-analogous-2-sm": "...",
        // ... другие тени
      },
      backgroundColor: {
        "pastel-analogous-2-alpha-10": "...",
        // ... другие прозрачности
      }
    }
  }
};
```

## Преимущества

- **Комплексность**: Все необходимые цвета для полноценного UI
- **Последовательность**: Автоматическая генерация гармоничных вариантов
- **Доступность**: Учитывает WCAG рекомендации
- **Производительность**: OKLCH цветовое пространство
- **Гибкость**: Можно кастомизировать базовый цвет

## Кастомизация

Измените базовый цвет в коде генератора:
```javascript
const baseColor = "#YOUR_COLOR"; // Вместо #8B8B8B
```

Или через API:
```javascript
// В generateExtendedPalette.ts можно добавить параметры
generateExtendedPalette(paletteName, baseColor, options)
```